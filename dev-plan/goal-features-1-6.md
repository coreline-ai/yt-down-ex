아래 프롬프트를 Goal에 그대로 사용하면 됩니다.
```markdown
목표: Stash Local의 기능 1~6을 개발 계획의 권장 순서대로 구현하고, 실제 Chrome에서 다운로드·영상 재생·기록 관리·재시도·업데이트 복구까지 검증한다.

작업 경로:
/Users/iriver/Documents/ChatGPT/yt-down-interface

기준 개발 계획:
/Users/iriver/Documents/ChatGPT/yt-down-interface/dev-plan/implement_20260910_164023.md

먼저 개발 계획, 적용되는 AGENTS.md, README, 안정화 검토 문서와 현재 코드를 확인한다. 계획과 실제 구현 상태를 대조하고, 이미 완료된 작업은 증거를 확인한 후 이어서 진행한다.

다음 순서를 준수한다.
1. 데이터·통신 계약 및 실제 Chrome 재생 연결 검증
2. MP4 호환 저장
3. 재생 화면·탐색·음량·배속·전체화면·이어보기
4. 다운로드 전 미리보기
5. 기록 검색·필터·선택 삭제·실패 기록 일괄 삭제
6. 일시적 오류 자동 재시도
7. 업데이트·진단·실패 복구
8. 전체 통합 테스트 및 실제 Chrome 인수 검증

진행 규칙:
- 각 Phase의 자체 테스트와 완료 조건을 충족한 뒤 다음 Phase로 이동한다.
- 실패하면 원인을 분석하고 수정한 후 같은 조건으로 재검증한다. 실패 보고만 하고 종료하지 않는다.
- 구현 과정의 일반적인 기술 판단은 자율적으로 수행한다. 사용자 결정이 필요한 중대한 범위 변경이나 되돌릴 수 없는 작업만 확인한다.
- 체크박스와 이슈 기록을 실제 진행 상태에 맞게 갱신한다.
- 기존 다운로드 파일·설정·작업 기록을 보존한다. 기록 삭제는 영상 파일 삭제와 구분한다.
- 구현한 코드를 빌드하고 필요한 로컬 엔진 재설치·확장 재로드까지 수행하여 사용 중인 Chrome에 적용한다.
- 추가 기능이나 무관한 리팩터링으로 범위를 확장하지 않는다.
- 진행 상황은 한국어로 간결하게 알린다.

필수 검증:
- MP4 호환 저장 결과의 실제 컨테이너·H.264 영상·AAC 음성을 확인한다. 무음 원본은 별도로 구분한다.
- ffprobe와 전체 디코딩 검사를 통과한 실제 파일을 확인한다.
- Computer Use로 실제 Chrome에서 주소 입력 → 분석 → 옵션 선택 → 다운로드 → 재생 → 탐색·배속·전체화면 → 닫기·이어보기를 검증한다.
- 기록 검색·일괄 삭제·다중 패널 동기화와 영상 파일 보존을 검증한다.
- 일시적 실패의 제한된 재시도, 취소, 재시작과 중복 저장 방지를 검증한다.
- 업데이트 성공·실패·롤백과 이후 다운로드·재생을 검증한다.
- 로컬 자동 테스트, 실제 Chrome 테스트, 외부 사이트 다운로드 결과를 구분한다.
- 외부 주소가 실패하면 다른 샘플의 성공으로 원래 문제가 해결됐다고 보고하지 않는다.
- 권한 거부 등 앱에서 해결할 수 없는 제한은 증거와 함께 명시한다. 접근 제한을 우회하거나 모든 사이트 지원을 주장하지 않는다.

완료 기준:
개발 계획의 기능 1~6과 모든 Phase의 필수 완료 조건을 실제 증거로 충족해야 한다. UI 표시, 모의 테스트, 파일 확장자, 다운로드 진행률 100%만으로 성공을 선언하지 않는다. 미실행·차단·실패가 남은 필수 항목은 완료 처리하지 않는다.

최종 결과물:
- 구현 및 적용된 앱
- 실제 상태로 갱신된 개발 계획
- 자동 테스트 로그와 실제 Chrome 검증 기록
- 다운로드 파일 경로·크기·코덱·길이·SHA-256 및 재생 증거
- 설치·업데이트·복구 안내
- 완료 기능, 검증 범위, 남은 제한을 정리한 한국어 최종 보고서

계획 제안에서 멈추지 말고 구현부터 검증까지 지속적으로 진행한다.
```
